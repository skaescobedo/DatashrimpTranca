
  # Continue with prompt

  This is a code bundle for Continue with prompt. The original project is available at https://www.figma.com/design/NDrhK2l56RtygJ6IB3uXGx/Continue-with-prompt.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  